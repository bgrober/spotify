'use strict';
var React = require('react');
var ReactDOM = require('react-dom');
var jquery = require('jquery');
var SignInHeader = React.createClass({

  render() {
    return (
      <div>
        <h1 id="title"> Train Smart, Train Together </h1>
      </div>
    );
  }
});

module.exports = SignInHeader;
